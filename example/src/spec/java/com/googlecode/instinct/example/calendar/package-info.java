/**
 * This package contains examples of:
 *
 * <br/>1 Defining specifications in a base class and having them run when a suclass is run
 * <br/>2 Defining a @BeforeSpecification method in a base class and having it run before each specification in the subclass and base class (if any)
 * <br/>3 Overriding a @BeforeSpecification in a subclass (overriding the base class version)
 * <br/>4 Defining @AfterSpecification methods in the base class and subclasses and causing both to run when a subclas spec is invoked
 * <br/>5 Use of private lifecycle methods
 *
 */
package com.googlecode.instinct.example.calendar;

